const Footer = () => {
  return (
    <footer className="border-t border-black/10">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-6 py-6 text-xs uppercase tracking-[0.2em] text-black/50 md:flex-row md:items-center md:justify-between">
        <span>Unfocused Ballers</span>
        <span>Humble in Victory.</span>
      </div>
    </footer>
  );
};

export default Footer;
